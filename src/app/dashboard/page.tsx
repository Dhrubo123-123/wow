import { PhasePlaceholder } from "@/components/PhasePlaceholder";

export default function DashboardPage() {
  return (
    <PhasePlaceholder
      title="Dashboard"
      description="Level, XP progress, current quest, streak, skill summary, achievements, and next milestone will appear here."
      phase="Phase 8"
    />
  );
}
