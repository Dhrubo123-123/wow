import { PhasePlaceholder } from "@/components/PhasePlaceholder";

export default function SkillsPage() {
  return (
    <PhasePlaceholder
      title="Skills"
      description="Visual skill tree (unlocked / partially unlocked / locked / mastery), driven by database config."
      phase="Phase 16"
    />
  );
}
