import { PhasePlaceholder } from "@/components/PhasePlaceholder";

export default function MentorPage() {
  return (
    <PhasePlaceholder
      title="AI Mentor"
      description="Ask what to do today, why you're stuck, or how close you are to your goal — backed by a compact context builder, not a raw database dump."
      phase="Phase 18"
    />
  );
}
