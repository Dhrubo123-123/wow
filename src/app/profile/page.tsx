import { PhasePlaceholder } from "@/components/PhasePlaceholder";

export default function ProfilePage() {
  return (
    <PhasePlaceholder
      title="Profile"
      description="Name, avatar, level, XP, current goal, skills, achievements, and streak will appear here once Supabase Auth is wired up."
      phase="Phase 3"
    />
  );
}
