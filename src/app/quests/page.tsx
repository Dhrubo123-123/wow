import { PhasePlaceholder } from "@/components/PhasePlaceholder";

export default function QuestsPage() {
  return (
    <PhasePlaceholder
      title="Quests"
      description="AI-generated quests and their lifecycle (available → accepted → in progress → submitted → under review → completed/failed) will appear here."
      phase="Phase 9"
    />
  );
}
